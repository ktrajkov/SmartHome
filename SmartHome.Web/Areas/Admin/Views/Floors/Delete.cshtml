@model SmartHome.Models.Floor

@{
    ViewBag.Title = "Delete";
}

<h3>Are you sure you want to delete this floor?</h3>
<fieldset>
    <legend>Floor</legend>

    <dl>
         <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</fieldset>
@using (Html.BeginForm())
{
    <div class="form-actions no-color">
        <input type="submit" value="Delete" class="btn" />      
    </div>
      @Html.ActionLink("Back to List", "List", "Floors", new { HouseId = Model.HouseId }, new { @class = "btn" })
}
